# Claude Instructions

This repository is Codex-first. Start with `AGENTS.md`, then read `docs/codex/README.md`.

Use the Codex handbook as the source of truth for PartitionPro business context, architecture, engineering standards, safety rules, task playbooks, and validation expectations.

Do not access production/cloud resources or expose real secrets. Preserve unrelated worktree changes.
