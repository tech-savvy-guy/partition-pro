info:
  name: get-users
  type: http
  seq: 34

http:
  method: GET
  url: "{{baseURL}}/security/v1.0/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
