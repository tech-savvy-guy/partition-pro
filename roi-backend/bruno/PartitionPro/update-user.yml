info:
  name: update-user
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseURL}}/security/v1.0/users/{{userId}}/update"
  body:
    type: json
    data: |-
      {
        "name": "{{update_user_name}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
