info:
  name: poll-partition-tree
  type: http
  seq: 43

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/workflows/{{case_id}}/{{partition_id}}/node/{{node_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
