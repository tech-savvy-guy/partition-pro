info:
  name: view-dataset-file
  type: http
  seq: 47

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/cases/{{case_id}}/datasets/{{dataset_id}}"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
