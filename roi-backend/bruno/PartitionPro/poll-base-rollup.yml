info:
  name: poll-base-rollup
  type: http
  seq: 44

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/workflows/{{case_id}}/{{partition_id}}/preview-rollup-status/{{task_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
