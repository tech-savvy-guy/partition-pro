info:
  name: dataset-versions
  type: http
  seq: 25

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/cases/{{case_id}}/datasets/POS/versions"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
