info:
  name: run-workflow
  type: http
  seq: 31

http:
  method: POST
  url: "{{baseURL}}/reports/v1.0/workflows/{{case_id}}/{{partition_id}}/process_workflow/run"
  headers:
    - name: X-Client-Timestamp
      value: 2025-12-01 01:38:07.360371+05:30
  body:
    type: json
    data: |-
      {
        "selected_sku_ids": {{selected_sku_ids}}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
