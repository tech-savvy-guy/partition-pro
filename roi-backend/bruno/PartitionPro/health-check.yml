info:
  name: health check
  type: http
  seq: 33

http:
  method: GET
  url: "{{baseURL}}/security/v1.0/health"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
