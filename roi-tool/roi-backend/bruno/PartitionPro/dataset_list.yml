info:
  name: dataset_list
  type: http
  seq: 24

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/cases/{{case_id}}/datasets"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
