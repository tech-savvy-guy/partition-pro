info:
  name: delete-user
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseURL}}/security/v1.0/users/{{userId}}/delete"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
