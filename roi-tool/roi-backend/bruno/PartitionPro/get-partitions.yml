info:
  name: get-partitions
  type: http
  seq: 19

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/cases/{{case_id}}/partitions"
  params:
    - name: partition_id
      value: "{{partition_id}}"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
