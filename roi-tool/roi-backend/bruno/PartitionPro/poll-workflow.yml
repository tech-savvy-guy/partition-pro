info:
  name: poll-workflow
  type: http
  seq: 32

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/workflows/{{case_id}}/{{partition_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
