info:
  name: get-roles
  type: http
  seq: 35

http:
  method: GET
  url: "{{baseURL}}/security/v1.0/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
