info:
  name: get-changelogs
  type: http
  seq: 45

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/changelogs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
