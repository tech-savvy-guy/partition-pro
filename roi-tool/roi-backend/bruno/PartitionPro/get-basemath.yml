info:
  name: get-basemath
  type: http
  seq: 30

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/cases/{{case_id}}/partitions/{{partition_id}}/base-math"
  headers:
    - name: x-ms-blob-type
      value: BlockBlob
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
