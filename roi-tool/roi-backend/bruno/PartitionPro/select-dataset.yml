info:
  name: select-dataset
  type: http
  seq: 26

http:
  method: POST
  url: "{{baseURL}}/reports/v1.0/cases/{{case_id}}/datasets/POS/versions/{{dataset_version_id}}/select"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
