info:
  name: get-skuselection
  type: http
  seq: 29

http:
  method: GET
  url: "{{baseURL}}/reports/v1.0/cases/{{case_id}}/partitions/{{partition_id}}/sku-selection"
  headers:
    - name: x-ms-blob-type
      value: BlockBlob
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
